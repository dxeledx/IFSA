# @package _global_

defaults:
  - override /method: ifsa
  - override /model: csp_lda
  - _self_

# Keep the historical variant name for report_matrix.
experiment_name: ifsa_v11_logmean_shrink0p1

method:
  mean_mode: logeuclid
  ref_subject_mean_mode: logeuclid
  cov_shrink_alpha: 0.1
  damp_mode: euclid_ema
  output_space: reference
  trigger_mode: fixed

