# @package _global_

# Base experiment (no overrides)

# Optional human-readable experiment tag. When set, it becomes the CSV/figure
# `variant` identifier and is included in output filenames.
experiment_name: null
