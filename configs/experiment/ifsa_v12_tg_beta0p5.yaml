# @package _global_

defaults:
  - override /method: ifsa
  - override /model: csp_lda
  - _self_

experiment_name: ifsa_v12_tg_beta0p5

method:
  target_beta: 0.5
  mean_mode: logeuclid
  ref_subject_mean_mode: logeuclid
  cov_trace_norm: true
  cov_shrink_alpha: 0.1
  lambda_damp: 0.0
  lr: 0.15
  lambda_u: 0.0
  output_space: reference

